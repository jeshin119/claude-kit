# <프로젝트 이름> — Report

| | |
|---|---|
| 회차 | N |
| 작성 | YYYY-MM-DD |
| 사유 | 종료 / 보류 |

<!-- TOC -->

이 문서는 끝나거나 멈춘 뒤의 판정을 담는다. 여기 나오는 숫자는 전부 `journal.md`
15 검증 기록에 실측값으로 있고, 목표값의 정의는 `charter.md` 6 성공 기준에 있다.

<!-- 생성 시 지침: 상태가 종료나 보류로 바뀔 때 만든다. 새 사실을 여기서 처음 쓰지
     않는다. 프로파일 M은 17과 18만 쓰고 19 참고 자료 절은 지운다. -->

## 17. 결과와 한계

### 성공 기준 판정

`charter.md` 6 성공 기준의 행 수와 아래 행 수가 같아야 한다. `판정` 칸은
`달성`·`미달`·`해당 없음` 중 하나다. `달성`은 `journal.md` 15 검증 기록의 `방법`이
`타입·컴파일`·`테스트(스펙 확인)`·`실행`·`정적분석` 중 하나일 때만 쓴다.

| ID | 목표값 | 실측값 | 판정 | 미달 사유 |
|---|---|---|---|---|
<!-- 예시: | SC-01 | 90초 이하 | 96초 (3회 평균) | 미달 | 캐시 미적중 구간이 남았다 | -->

### 한계

이번 구현이 못 하는 것과 알려진 이슈.

| 항목 | 영향 | 우회 방법 |
|---|---|---|
| | | |

## 18. 미결과 다음 단계

**답을 찾지 못한 질문**

-

**다음 회차 후보**

| 항목 | 이번에 하지 않은 이유 |
|---|---|
| | |

**멈춘 이유와 재개 조건**

보류일 때만 쓴다. 무엇 때문에 멈췄고, 무엇이 갖춰지면 다시 여는가. `decisions/`에
`상태: 미결`로 남은 결정이 있으면 여기 그대로 옮긴다.

**되짚을 것**

무엇을 잘못 판단했나. 다시 한다면 어디서 달라지는가.

## 19. 참고 자료 `L`

한 줄 요약이 없는 링크는 넣지 않는다.

| 자료 | 여기서 얻은 것 |
|---|---|
| | |
