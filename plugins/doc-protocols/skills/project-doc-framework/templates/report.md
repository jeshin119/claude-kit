# <프로젝트 이름> — Report

| | |
|---|---|
| 회차 | N |
| 작성 | YYYY-MM-DD |
| 사유 | 종료 / 보류 |

<!-- TOC -->

이 문서는 **끝나거나 멈춘 뒤의 판정**을 담는다. 여기 나오는 숫자는 전부 `journal.md`
15에 실측값으로 이미 있어야 하고, 새 사실을 여기서 처음 쓰지 않는다. 목표값의 정의는
`charter.md` 6에 있다.

<!-- 생성 시 지침: 상태가 종료나 보류로 바뀔 때 만든다. 프로파일 M 은 17 과 18 만 쓰고
     19 참고 자료 절은 지운다. 이 주석은 생성 후 지운다. -->

## 17. 결과와 한계

### 성공 기준 판정

`charter.md` 6의 행 수와 아래 행 수가 같아야 한다.

| ID | 목표값 | 실측값 | 판정 | 미달 사유 |
|---|---|---|---|---|
<!-- 예시: | SC-01 | 90초 이하 | 96초 (3회 평균) | 미달 | 캐시 미적중 구간이 남았다 | -->

`판정` 칸은 `달성`·`미달`·`해당 없음` 중 하나로 확정한다. 미확정 상태를 남기지 않는다.

`달성`은 `journal.md` 15의 `방법` 칸에 `타입·컴파일`·`테스트(스펙 확인)`·`실행`·
`정적분석` 중 하나가 있을 때만 쓴다. `테스트(미확인)`과 `LLM 검토`뿐이면 스펙을 아무도
확인하지 않은 것이라 달성으로 판정하지 않는다.

### 한계

이번 구현이 못 하는 것. 알려진 이슈.

| 항목 | 영향 | 우회 |
|---|---|---|
| | | |

## 18. 미결과 다음 단계

**답을 못 찾은 질문**

-

**다음 회차 후보**

| 항목 | 왜 이번에 안 했나 |
|---|---|
| | |

**멈춘 이유와 재개 조건**

보류일 때만 쓴다. 무엇 때문에 멈췄고, 무엇이 갖춰지면 다시 여는가.
`decisions/` 에 `상태: 미결` 로 남은 결정이 있으면 여기 그대로 옮긴다. 재개할 때
제일 먼저 막는 지점이다.

**되짚을 것**

무엇을 잘못 판단했나. 다시 한다면 어디서 갈라질까.

## 19. 참고 자료 `L`

한 줄 요약이 없는 링크는 넣지 않는다.

| 자료 | 여기서 얻은 것 |
|---|---|
| | |
