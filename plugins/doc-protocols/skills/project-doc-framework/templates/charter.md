# <프로젝트 이름> — Charter

| | |
|---|---|
| 프로파일 | M / L |
| 시작 | YYYY-MM-DD |
| 상태 | 진행 / 보류 / 종료 |

<!-- TOC -->

이 문서는 무엇을 해결하는지와 어떻게 해결하는지(1~6), 무엇을 만들기로 합의했는지(7~9)를
담는다. 설계는 `design.md`, 결정 근거는 `decisions/`, 7~9가 바뀐 이력은 `journal.md`
16 변경 이력에 있다.

<!-- 생성 시 지침: 헤더의 프로파일 칸은 `M`이나 `L` 한 글자만 남긴다. 둘 다 남으면
     check-trace.sh에서 템플릿 잔재로 검출된다. `L` 표시 절은 L 프로파일부터 채우고
     그 전에는 절을 지운다. 상태를 보류나 종료로 바꿀 때 report.md를 만든다. 각 절의
     `생성 시 지침` 주석과 `예시:` 주석은 생성 후 전부 지운다. -->

## 1. 문제와 배경

<!-- 생성 시 지침: 지금 무엇이 안 되고 있는가. 그대로 두면 무슨 손해인가. 지금 해야
     하는 이유는 무엇인가. 이 절만 서술체로 쓴다. 순서는 상황, 사례, 원인이고, 그
     분야를 모르는 사람이 읽고 따라올 수 있어야 한다. 용어는 처음 나오는 자리에서 한
     문장으로 설명한다. 어떻게 해결하는지는 2에 쓴다. 2를 만들지 않는 프로젝트는 이
     절 끝에 명세의 출처를 한 줄 남긴다. -->

## 2. 해결 방법

<!-- 생성 시 지침: 해결 방법이 밖에서 주어진 프로젝트(외주, 명세 구현, 이관)는 이 절을
     통째로 지운다. 판정 기준은 카탈로그의 2번 항목에 있다. 쓰기로 했으면 문제가
     생기는 과정의 어느 단계에서 어떻게 개입하는지를 여기서 한 번만 설명한다. 개별
     요구사항은 7, 구현 구조는 design.md 11에 둔다. 단계가 하나뿐이면 표 없이 두
     문장으로 끝낸다. 핵심 메커니즘의 부작용과 그것을 감당하는 장치도 적고, 그 장치가
     성립 조건이면 어느 SC-nn으로 판정하는지 잇는다. -->

| 단계 | 하는 일 | 요구사항 |
|---|---|---|
<!-- 예시: | 1 | 입력 출처를 세션에 기록한다 | FR-01 | -->

**성립 전제**

아래가 참이어야 위 해결 방법이 성립한다. 개별 결정의 전제는 `decisions/`에 있다.

| 전제 | 깨지면 | 대응 |
|---|---|---|
<!-- 예시: | 도구 목록이 세션 중 고정이다 | 우회 경로가 생긴다 | NFR-01 | -->

## 3. 범위와 비범위

**이번에 하는 것**

-

**이번에 하지 않는 것**

| 항목 | 제외 이유 |
|---|---|
| | |

## 4. 사용자와 이해관계자 `L`

| 역할 | 누구 | 이 문서에서 보는 것 |
|---|---|---|
| | | |

## 5. 용어 `L`

<!-- 생성 시 지침: 성격이 다른 셋을 한 표에 섞지 않는다. 어느 칸도 TODO로 채우지
     않는다. 이름을 아직 정하지 않은 용어는 두 번째 표에 두고 정해지면 첫 표로 옮긴다.
     첫 표의 오른쪽 칸은 9의 계약과 어긋나면 안 된다. 약어는 본문에서 쓰는 것을
     빠짐없이 넣고 초출 위치에도 원어를 한 번 병기한다. -->

**코드나 설정에 이름이 있는 용어**

| 용어 | 뜻 | 코드에서의 이름 |
|---|---|---|
| | | |

**코드에 이름이 없는 용어**

| 용어 | 뜻 |
|---|---|
| | |

**약어**

| 약어 | 원어 | 이 문서에서 |
|---|---|---|
| | | |

## 6. 성공 기준

<!-- 생성 시 지침: 숫자와 판정 방법이 있어야 한다. 현재값을 아직 측정하지 못했으면
     `미측정`이라고 적고 빈 칸으로 두지 않는다. -->

각 기준의 실측값은 `journal.md` 15 검증 기록에, 판정은 `report.md` 17 결과와 한계에
있다.

| ID | 기준 | 현재값 | 목표값 | 판정 방법 |
|---|---|---|---|---|
<!-- 예시: | SC-01 | 빌드 시간 | 210초 | 90초 이하 | `time make build` 3회 평균 | -->

## 7. 기능 요구사항

<!-- 생성 시 지침: 주체를 명시하고 `~해야 한다`로 끝낸다. 한 항목에 한 가지만
     요구한다. -->

| ID | 요구사항 |
|---|---|
<!-- 예시: | FR-01 | 사용자는 승인 없이 조회 도구를 호출할 수 있어야 한다 | -->

## 8. 제약과 비기능 요구사항

<!-- 생성 시 지침: 성능·용량·보안·권한·비용·호환성·기한, 그리고 바꿀 수 없는 외부
     조건. 출처 칸에는 그 제약이 어디서 왔는지를 적는다 (법규명, 발주 문서,
     ADR-nnnn). 왜 받아들였는지는 decisions/에 쓴다. -->

| ID | 내용 | 출처 |
|---|---|---|
<!-- 예시: | NFR-01 | 추가 지연은 호출당 100ms를 넘지 않아야 한다 | 발주 요건 3.2 | -->

## 9. 인터페이스와 데이터 계약 `L`

<!-- 생성 시 지침: 바꾸면 다른 사람의 코드가 깨지는 것만 적는다. API 시그니처, 스키마,
     파일 포맷, 이벤트 페이로드, 설정 키. 내부 자료구조는 design.md 11에 둔다. -->
