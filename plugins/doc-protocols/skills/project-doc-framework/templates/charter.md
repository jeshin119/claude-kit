# <프로젝트 이름> — Charter

| | |
|---|---|
| 프로파일 | S / M / L |
| 시작 | YYYY-MM-DD |
| 상태 | 진행 / 보류 / 종료 |

<!-- TOC -->

이 문서는 무엇을 푸는지와 어떻게 풀지(1~6), 무엇을 만들기로 합의했는지(7~9)를
담는다. 설계는 `design.md`, 결정 근거는 `decisions/`에 있다. 7~9를 바꾸려면 근거를
`decisions/`에 남기고 `journal.md` 16에 기록한다.

<!-- 생성 시 지침: `M` `L` 표시 항목은 해당 프로파일부터 채운다. 그 전에는 절을
     만들지 않는다. 이 주석과 표시는 생성 후 지운다. -->

## 1. 문제와 배경

지금 무엇이 안 되고 있는가. 그대로 두면 무슨 손해인가. 왜 지금인가.

이 절만 서술체를 쓴다. 요구사항체로 쓰면 결론만 나열되어 배경이 전달되지 않는다.
**대상 분야를 모르는 사람이 읽고 따라올 수 있어야 한다.** 순서는 상황 → 사례 →
원인이고, 절이 길어지면 h3로 나눈다. 용어는 처음 나오는 자리에서 한 문장으로 푼다.
5 용어 표에 있다는 이유로 본문에서 설명을 생략하지 않는다. 어떻게 푸는지는 2에 쓴다.

## 2. 해결 방법 `M`

<!-- 생성 시 지침: 해결 방법이 밖에서 주어진 프로젝트(외주, 명세 구현, 이관)는 이 절을
     통째로 지운다. 대신 1 끝에 명세의 출처를 한 줄 남긴다. 판정 기준은 카탈로그의 2번
     항목에 있다. 이 주석은 생성 후 지운다. -->

문제의 어느 고리를 어디서 끊는가. 판정이나 개입이 일어나는 지점을 한 곳으로 지목한다.
메커니즘은 여기서 한 번만 설명하고, 개별 요구사항은 7, 구현 구조는 `design.md` 11에
둔다. 단계가 하나뿐이면 표 없이 두 문장으로 끝낸다.

| 단계 | 하는 일 | 요구사항 |
|---|---|---|
| | | FR-01 |

핵심 메커니즘만으로 부족한 지점을 이어서 적는다. 그 메커니즘의 부작용은 무엇이고
무엇으로 감당하는가. 감당하는 쪽이 부가 기능이 아니라 성립 조건이면 그렇게 밝히고,
어느 `SC-nn`으로 판정하는지 잇는다.

**성립 전제** — 이게 참이어야 위 해결 방법이 성립한다. 개별 결정의 전제는 `decisions/`에 둔다.

| 전제 | 깨지면 | 대응 |
|---|---|---|
| | | NFR-01 |

## 3. 범위와 비범위 `M`

**이번에 하는 것**

-

**이번에 하지 않는 것**

| 항목 | 제외 이유 |
|---|---|
| | |

## 4. 사용자와 이해관계자 `L`

| 역할 | 누구 | 이 문서에서 뭘 보는가 |
|---|---|---|
| | | |

## 5. 용어 `L`

성격이 다른 셋을 한 표에 섞지 않는다. 어느 칸도 `TODO`로 채우지 않는다.

**코드·설정에 이름이 있는 용어**

| 용어 | 뜻 | 코드에서의 이름 |
|---|---|---|
| | | |

오른쪽 칸은 9의 계약과 어긋나면 안 된다.

**코드에 이름이 없는 용어**

지표나 평가 단위처럼 식별자가 될 일이 없는 용어, 그리고 이름을 아직 정하지 않은
용어를 여기 둔다. 이름이 정해지면 위 표로 옮긴다.

| 용어 | 뜻 |
|---|---|
| | |

**약어**

본문에서 쓰는 약어를 빠짐없이 넣는다. 초출 위치에도 원어를 한 번 병기한다.

| 약어 | 원어 | 이 문서에서 |
|---|---|---|
| | | |

## 6. 성공 기준

숫자와 판정 방법이 있어야 한다. 여기 있는 각 행은 `journal.md` 15에 실측값이,
`report.md` 17에 판정이 있어야 한다.

| ID | 기준 | 현재값 | 목표값 | 판정 방법 |
|---|---|---|---|---|
| SC-01 | | | | |

## 7. 기능 요구사항

주체를 명시하고 `~해야 한다`로 끝낸다. 한 항목에 한 가지만 요구한다.

| ID | 요구사항 |
|---|---|
| FR-01 | |

## 8. 제약과 비기능 요구사항 `M`

성능·용량·보안·권한·비용·호환성·기한, 그리고 바꿀 수 없는 외부 조건.

| ID | 내용 | 근거 |
|---|---|---|
| NFR-01 | | |

## 9. 인터페이스와 데이터 계약 `L`

바꾸면 남의 코드가 깨지는 표면만 적는다.
