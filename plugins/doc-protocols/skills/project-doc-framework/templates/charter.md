# <프로젝트 이름> — Charter

| | |
|---|---|
| 프로파일 | S / M / L |
| 시작 | YYYY-MM-DD |
| 상태 | 진행 / 보류 / 종료 |

<!-- TOC -->

이 문서는 무엇을 푸는지(1~5)와 무엇을 만들기로 합의했는지(6~8)를 담는다. 설계는
`design.md`, 결정 근거는 `decisions/`에 있다. 6~8을 바꾸려면 근거를 `decisions/`에
남기고 `journal.md` 15에 기록한다.

<!-- 생성 시 지침: `M` `L` 표시 항목은 해당 프로파일부터 채운다. 그 전에는 절을
     만들지 않는다. 이 주석과 표시는 생성 후 지운다. -->

## 1. 문제와 배경

지금 무엇이 안 되고 있는가. 그대로 두면 무슨 손해인가. 왜 지금인가.

이 절만 서술체를 쓴다. 요구사항체로 쓰면 결론만 나열되어 배경이 전달되지 않는다.
**대상 분야를 모르는 사람이 읽고 따라올 수 있어야 한다.** 순서는 상황 → 사례 →
원인 → 접근이고, 절이 길어지면 h3로 나눈다. 용어는 처음 나오는 자리에서 한 문장으로
푼다. 4 용어 표에 있다는 이유로 본문에서 설명을 생략하지 않는다.

## 2. 범위와 비범위 `M`

**이번에 하는 것**

-

**이번에 하지 않는 것**

| 항목 | 제외 이유 |
|---|---|
| | |

## 3. 사용자와 이해관계자 `L`

| 역할 | 누구 | 이 문서에서 뭘 보는가 |
|---|---|---|
| | | |

## 4. 용어 `L`

| 용어 | 뜻 | 코드에서의 이름 |
|---|---|---|
| | | |

## 5. 성공 기준

숫자와 판정 방법이 있어야 한다. 여기 있는 각 행은 `journal.md` 14에 실측값이,
`report.md` 16에 판정이 있어야 한다.

| ID | 기준 | 현재값 | 목표값 | 판정 방법 |
|---|---|---|---|---|
| SC-01 | | | | |

## 6. 기능 요구사항

주체를 명시하고 `~해야 한다`로 끝낸다. 한 항목에 한 가지만 요구한다.

| ID | 요구사항 |
|---|---|
| FR-01 | |

## 7. 제약과 비기능 요구사항 `M`

성능·용량·보안·권한·비용·호환성·기한, 그리고 바꿀 수 없는 외부 조건.

| ID | 내용 | 근거 |
|---|---|---|
| NFR-01 | | |

## 8. 인터페이스와 데이터 계약 `L`

바꾸면 남의 코드가 깨지는 표면만 적는다.
