# ADR-NNNN. <결정을 한 줄로>

| | |
|---|---|
| 날짜 | YYYY-MM-DD |
| 상태 | 미결 / 유효 / 폐기 (ADR-NNNN이 대체) |
| 되돌리기 | 어렵다 / 쉽다 |
| 관련 | FR-nn, NFR-nn |

<!-- TOC -->

이 문서는 결정 하나와 그 근거를 담으며, 뒤집힌 결정도 지우지 않고 상태만 `폐기`로
바꾼다. 요구사항 ID의 정의는 `../charter.md`에 있다.

<!-- 생성 시 지침: 되돌리기 쉬운 결정(이름, 패키지 구조, 로깅 포맷)은 ADR로 쓰지
     않는다. 답이 아직 없으면 `상태: 미결`로 두고 "정해질 때까지 미루는 것"만
     채운다. 유효로 확정되면 그 절을 지운다. -->

## 결정 내용

한 문장으로 쓴다. 아직 답이 없으면 무엇을 정해야 하는지를 한 문장으로 쓴다.

## 정해질 때까지 미루는 것

`상태: 미결`일 때만 두는 절로, 이 결정에 걸린 작업을 적는다.

| 미루는 작업 | 정하는 데 필요한 것 |
|---|---|
| | |

## 배경과 제약

이 결정을 강제한 조건. 무엇을 몰랐고 무엇이 급했는가.

## 선택지

| 안 | 장점 | 단점 | 채택 |
|---|---|---|---|
| A | | | O |
| B | | | X |

## 선택 이유

## 포기한 것

채택안이 못 하는 것. 나중에 이 결정을 의심하게 될 지점.

## 전제

아래가 참이어야 이 결정이 성립한다.

| 전제 | 깨지면 |
|---|---|
| | |
