# ADR-NNNN. <결정을 한 줄로>

| | |
|---|---|
| 날짜 | YYYY-MM-DD |
| 상태 | 미결 / 유효 / 폐기 (ADR-NNNN이 대체) |
| 되돌리기 | 어렵다 / 쉽다 |
| 관련 | FR-nn, NFR-nn |

<!-- TOC -->

이 문서는 결정 하나와 그 근거를 담는다. 뒤집힌 결정도 지우지 않고 상태만 `폐기`로
바꾼다. 요구사항 ID의 정의는 `../charter.md`에 있다.

<!-- 생성 시 지침: 되돌리기 쉬운 결정(이름, 패키지 구조, 로깅 포맷)은 ADR 로 쓰지
     않는다. 답이 아직 없으면 `상태: 미결` 로 두고 아래 "정해질 때까지 미루는 것"만
     채운다. 이 주석은 생성 후 지운다. -->

## 무엇을 정했나

한 문장. 아직 답이 없으면 무엇을 정해야 하는지를 한 문장으로 쓴다.

## 정해질 때까지 미루는 것

`상태: 미결` 일 때만 쓴다. 이 결정에 걸린 작업을 적는다. 정하지 않고 진행하면 그
결정이 코드 전체에 퍼진 다음에 드러난다.

| 미루는 작업 | 정하는 데 필요한 것 |
|---|---|
| | |

## 어떤 상황이었나

이 결정을 강제한 조건. 무엇을 몰랐고 무엇이 급했는가.

## 선택지

| 안 | 장점 | 단점 | 채택 |
|---|---|---|---|
| A | | | O |
| B | | | X |

## 왜 이걸 골랐나

## 무엇을 포기했나

채택안이 못 하는 것. 나중에 이 결정을 의심하게 될 지점.

## 전제

이게 참이어야 이 결정이 성립한다.

| 전제 | 깨지면 |
|---|---|
| | |
