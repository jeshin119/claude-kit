# <프로젝트 이름> — Design

| | |
|---|---|
| 갱신 | YYYY-MM-DD |

<!-- TOC -->

이 문서는 **현재 설계 상태만** 담는다. 지난 설계와 바뀐 이유는 `journal.md` 와
`decisions/` 에 있다. 요구사항 ID(FR·NFR·SC)의 정의는 `charter.md` 에 있다.

## 11. 아키텍처

### 구성

<!-- 생성 시 지침: 구성 요소와 관계, 데이터가 어디서 어디로 흐르는가. 구성 요소가 넷을
     넘거나, 한 요청이 지나는 단계가 셋을 넘거나, 상태 전이에 조건이 붙으면 archify
     스킬로 그려 ../diagrams/ 에 두고 여기서 링크한다. 글 설명을 그림으로 대체하지
     않는다. -->

### 기술 스택

| 이름 | 버전 | 용도 | ADR |
|---|---|---|---|
| | | | |

<!-- 생성 시 지침: 선택 이유는 적지 않고 ADR 열로 링크한다. -->

## 12. 구현 현황

`상태` 칸은 `완료`·`진행`·`미착수` 중 하나다.

| FR | 무엇 | 어디 | 상태 |
|---|---|---|---|
<!-- 예시: | FR-01 | 세션 시작 시 출처 기록 | `src/atg/session.py` | 완료 | -->

**요구사항 없이 구현된 것** — 범위에 넣을지 뺄지 판단할 대상이다.

| 무엇 | 어디 | 왜 요구사항이 없는가 |
|---|---|---|
| | | |

### 착수 순서

<!-- 생성 시 지침: 미착수 항목의 순서와 그 근거만 둔다. 계획이 바뀌면 덮어쓴다. 순서는
     걸리는 시간이 아니라 끝났을 때 맞는지 짧게 확인할 수 있는가로 정한다. 확인할 수
     없으면 더 쪼개거나 확인 수단을 먼저 만든다. 기한과 예산은 charter.md 8, 이번에
     안 하기로 한 것은 charter.md 3 에 둔다. 일정 파일을 따로 만들지 않는다. -->

| 순서 | FR | 무엇 | 무엇으로 확인하나 | 착수 조건 |
|---|---|---|---|---|
<!-- 예시: | 1 | FR-01 | 입구부터 저장소까지 한 줄 관통 | 실제 요청 1건의 응답 | — | -->

## 13. 운영과 관측

### 실행

```bash
```

### 설정

| 키 | 기본값 | 뜻 |
|---|---|---|
| | | |

### 배포와 롤백

### 보는 것

| 지표·로그 | 어디서 | 이상 판정 |
|---|---|---|
| | | |
